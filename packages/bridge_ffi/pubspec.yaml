name: nativeflow_bridge_ffi
description: Monorepo shim — use package:nativeflow_bridge/ffi.dart on pub.dev.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.7.0 <4.0.0"

resolution: workspace

dependencies:
  nativeflow_bridge:
    path: ../nativeflow_bridge
