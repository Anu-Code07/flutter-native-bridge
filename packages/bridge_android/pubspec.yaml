name: nativeflow_bridge_android
description: Monorepo shim — platform plugin is in package:nativeflow_bridge.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  nativeflow_bridge:
    path: ../nativeflow_bridge
