name: nativeflow_bridge_generator
description: Source generation engine for NativeFlow Bridge Dart, Kotlin, and Swift contracts.
version: 0.1.0
repository: https://github.com/nativeflow/nativeflow_bridge

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  analyzer: ^12.0.0
  build: ^4.0.6
  nativeflow_bridge_annotations: ^0.1.0
  nativeflow_bridge_core: ^0.1.0
  source_gen: ^4.2.3

dev_dependencies:
  build_test: ^3.2.1
  test: ^1.26.3
