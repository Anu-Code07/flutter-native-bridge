name: nativeflow_bridge_devtools
description: Monorepo shim — use package:nativeflow_bridge/devtools.dart on pub.dev.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=3.27.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  nativeflow_bridge:
    path: ../nativeflow_bridge
