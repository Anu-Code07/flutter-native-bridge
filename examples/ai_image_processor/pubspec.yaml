name: nativeflow_ai_image_processor
description: Example FFI image processor bridge built with NativeFlow Bridge.
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  nativeflow_bridge_annotations:
    path: ../../packages/bridge_annotations
  nativeflow_bridge_ffi:
    path: ../../packages/bridge_ffi

dev_dependencies:
  build_runner: ^2.4.15
  nativeflow_bridge_generator:
    path: ../../packages/bridge_generator
