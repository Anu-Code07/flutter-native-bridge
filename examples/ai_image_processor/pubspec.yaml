name: nativeflow_ai_image_processor
description: Example FFI image processor bridge built with NativeFlow Bridge.
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  nativeflow_bridge_annotations: ^0.1.0
  nativeflow_bridge_ffi: ^0.1.0

dev_dependencies:
  build_runner: ^2.4.15
  nativeflow_bridge_generator: ^0.1.0
