name: nativeflow_payment_sdk_wrapper
description: Example payment SDK wrapper built with NativeFlow Bridge.
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  nativeflow_bridge:
    path: ../../packages/nativeflow_bridge

dev_dependencies:
  build_runner: ^2.4.15
