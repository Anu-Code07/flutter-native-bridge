name: nativeflow_kyc_document_capture
description: Example KYC document capture bridge built with NativeFlow Bridge.
publish_to: none

environment:
  sdk: ">=3.7.0 <4.0.0"

resolution: workspace

dependencies:
  nativeflow_bridge:
    path: ../../packages/nativeflow_bridge

dev_dependencies:
  build_runner: ^2.4.15
